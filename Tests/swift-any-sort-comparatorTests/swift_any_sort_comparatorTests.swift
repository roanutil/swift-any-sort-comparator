import XCTest
@testable import swift_any_sort_comparator

final class swift_any_sort_comparatorTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_any_sort_comparator().text, "Hello, World!")
    }
}
